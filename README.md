Requirements

to install modules:
'git submodule update --init --recursive'

- neovim latest
- lua-language-server
- lazygit command line tool (optional)
- kitty
- python-language-server (with all optionals)
- stylua
- something else?

How to install

1. Install uv
2. Clone repo to ~/.config/nvim/
3. Do 'uv sync' in repo
3. Launch neovim

What is still missing
- copy paste stays in buffer // I think this might already work?
- lock terminal windows so they wont fuck up
- lsp for terraform / terragrunt
- lsp for webdev
- better completions?
- templates? meaning snippets
- genai?
